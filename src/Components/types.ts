
interface EligibleVehicles {
    name : string,
    total_no: Number,
    maxDistance: Number,
    speed: Number
}

interface PlanetType {
    name: string,
    distance: number
}

interface Vehicle {
    name : string,
    total_no: Number,
    maxDistance: Number,
    speed: Number
}

export type {EligibleVehicles, PlanetType, Vehicle} 